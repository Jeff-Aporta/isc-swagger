/**
 * <sw-method> — chip del método HTTP.
 *
 * Ancho fijo ≈ `DELETE` (verbo más largo) para alinear la columna de títulos
 * sin el sobrante de un rem arbitrario grande.
 */
declare const SwMethod: CustomElementConstructor;
export { SwMethod };
