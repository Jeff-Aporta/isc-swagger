{
  "name": "isc-swagger",
  "version": "1.0.0",
  "private": true,
  "description": "Visor OpenAPI 3 de InSoft en web components (is-*). Sin React, sin MUI, sin Babel: TS -> ES modules planos + CDN is-webcomponents.",
  "type": "module",
  "homepage": "https://jeff-aporta.github.io/isc-swagger/",
  "author": "Jeff-Aporta",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.ts",
    "dev": "node scripts/build.ts --watch",
    "serve": "npx --yes serve . -l 4190",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.0"
  }
}
