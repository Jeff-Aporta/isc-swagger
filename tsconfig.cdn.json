{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ES2022",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "declaration": true,
    "emitDeclarationOnly": true,
    "outDir": "dist/cdn",
    "rootDir": "src"
  },
  "//": "Emite un .d.ts junto a cada .js publicado. Antes solo cubria los dos entry points que el kit expone; se amplio a todo src/ para que los tests, que prueban dist/cdn como caja negra, no vean `any` en lo que importan.",
  "include": [
    "src/js/**/*.ts",
    "src/components/**/*.ts",
    "src/types/**/*.d.ts"
  ]
}
